<div class="container">
  <div class="row">
    <div class="col md-12">
      <h3>商品一覧</h3>(全<%= @student_post.count %>件)
    </div>
  </div>
</div>

<div class="container">
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% @student_post.each do |student| %>
      <%= student.field %>
    <% end %>
  </div>
</div>