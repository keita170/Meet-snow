<div class="student-title mb-5">
  <h1 class="text-center">〜教わりたい方人募集中〜</h1>
  <div class="col-md-6 offset-md-4">
    <div class="row">
    <%= form_with url: teacher_posts_path, method: :get, local:true do |f| %>
      <%= f.select :field, options_for_select({"分野検索" => "分野検索","フリースタイル" => "フリースタイル", "グラトリ" => "グラトリ", "カービング" => "カービング", "その他" => "その他"}) %>
      <%= f.submit '検索', class:"btn btn-sm btn-outline-dark mr-5" %>
    <% end %>

    <%= form_with url: search_teacher_path, method: :get, local:true do |f| %>
      <%= f.text_field :key_word, value: @key_word, placeholder:'キーワード検索', class:'search_word' %>
      <%= f.submit '検索', class:"btn btn-sm btn-outline-dark"%>
    <% end %>
    </div>
  </div>
</div>

  <div class="row ml-5">
    <!--画面左側の新規投稿ページへのリンクがある範囲-->
    <div class="student-left col-md-2">
      <%= link_to new_teacher_post_path do %>
            <i class="fas fa-plus-square text-dark fa-2x new-button"></i>
            <span class="h2 text-dark">新規投稿する</span>
      <% end %>
    </div>
    <!--画面真ん中の一覧表示がされてる範囲-->
    <div class="student-center col-md-6 overflow-auto" style="width:100%; height:800px;">
      <!--投稿一覧 ここから-->
      <div class="teacher_post-tbody">
        <%= render 'teacher_posts/index', teacher_post: @teacher_post, comment_teacher: @comment_teacher %>
      </div>
      <!--投稿一覧 ここまで-->

      <!--ーーーーーーーーーーーーーーーーーーーーーーここから下は試しに書いてるだけーーーーーーーーーーーーーーーーーーーーーーーーー-->
      <p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p><p>O</p>
      <!--ーーーーーーーーーーーーーーーーーーーーーーここから上は試しに書いてるだけーーーーーーーーーーーーーーーーーーーーーーーーー-->
    </div>
    <div class="col-md-4">
      <h4>週間いいね獲得ランキング</h4>
      <% @favorite_rank.each.with_index(1) do |teacher, i| %>
      <p>第<%= i %>位<%= teacher.title %>:<%= teacher.favorite_teachers.count %>いいね</p>
      <% end %>
      <h4>週間コメント獲得ランキング</h4>
      <% @comment_rank.each.with_index(1) do |teacher, i| %>
      <p>第<%= i %>位<%= teacher.title %>:<%= teacher.comment_teachers.count %>コメント</p>
      <% end %>

      <h4>週間投稿数ランキング</h4>
      <% @ranking_users.each.with_index(1) do |user, i| %>
      <p>第<%= i %>位<%= user.name %>:<%= user.teacher_posts.count %>件</p>
      <% end %>
    </div>
  </div>
